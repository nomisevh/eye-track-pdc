def handle_rocket(*, dataset, intention, model_config, train_config, seed):
    # TODO
    if intention == 'train':
        # Create dataloader...
        ...
    else:
        raise NotImplementedError


def handle_inception_time(*, dataset, intention, model_config, train_config, device, seed):
    # TODO
    raise NotImplementedError


def handle_inception_former(*, dataset, intention, model_config, train_config, device, seed):
    # TODO
    raise NotImplementedError
